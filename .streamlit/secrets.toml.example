# Copy to .streamlit/secrets.toml for local use, or paste into
# Streamlit Cloud -> App settings -> Secrets.
#
# OPTIONAL. The app runs fully without a key using the local extractive
# backend; a key only enables the hosted-LLM comparison.
PACIFYIQ_GROQ_API_KEY = "gsk_your_key_here"
