# --- core (production, deploys to Streamlit Cloud) ---
numpy>=1.26
pydantic>=2.6
pydantic-settings>=2.2
pandas>=2.2
python-dotenv>=1.0

# --- LLM (Phase 7) ---
# groq is OPTIONAL: the local backend needs no key and no network
groq>=0.11

# --- retrieval (Phase 5/6) ---
rank-bm25>=0.2.2
tiktoken>=0.7
pdfplumber>=0.11
faiss-cpu>=1.8      # optional; NumPy brute force is the default at this corpus size

# --- multimodal (Phase 8) ---
Pillow>=10.0
pytesseract>=0.3.10   # needs the tesseract-ocr binary: apt install tesseract-ocr

# --- classification (Phase 4) ---
scikit-learn>=1.4
joblib>=1.3

# --- UI (Phase 13) ---
streamlit>=1.40

# NOTE: no torch here. Transformers stay in requirements-dev.txt so the
# deployed app fits inside Streamlit Cloud's ~1GB limit.

# --- data generation: needed at runtime because artifacts are built
# on first run rather than committed (see scripts/ensure_artifacts.py) ---
Faker>=25.0
